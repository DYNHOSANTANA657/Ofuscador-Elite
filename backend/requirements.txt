fastapi==0.116.1
uvicorn==0.35.0
python-multipart==0.0.20
keyring==25.6.0
piper-tts==1.4.2
# onnxruntime padrão. No macOS essa build já traz o CoreMLExecutionProvider, que o app
# testa em runtime (com sonda) e usa se der certo, senão volta pro CPU. No Windows não
# usamos DirectML: ele não executa as camadas de convolução de Fourier (FFC) do LaMa.
onnxruntime==1.28.0
numpy==2.2.6
opencv-python==4.12.0.88
rapidocr==3.9.1
